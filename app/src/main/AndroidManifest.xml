<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.multimediaapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MultimediaApp">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".AudioActivity" android:exported="true"/>
        <activity android:name=".VideoViewActivity" android:exported="true"/>
        <activity android:name=".VideoSurfaceViewActivity" android:exported="true"/>
        <activity android:name=".VideoActivity" android:exported="true"/>
        <activity android:name=".PhotoActivity" android:exported="true"/>

    </application>

</manifest>
